import { Box, Typography } from '@mui/material';

const ClaimEventToken: React.FC = () => {
  return (
    <Box>
      <Typography variant="h1">Claim Event Token</Typography>
    </Box>
  );
};

export default ClaimEventToken;